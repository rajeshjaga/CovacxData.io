import useFetch from "../../Api";

const sorter = () => {
  let day = [sunday, monday, tuesday, wednesday, thursday, friday, saturday];
  // let monday;
  // let tuesday;
  // let wednesday;
  // let thursday;
  // let friday;
  // let saturday;
  // let sunday;
  // const { data, loading, error } = useFetch(
  //   "https://arimamodel.herokuapp.com/vaccination_prediction"
  // );
  // const { dataPeo, loadingpeo, errorpeo } = useFetch(
  //   "https://arimamodel.herokuapp.com/people_prediction"
  // );
  // if (data !== undefined || dataPeo !== null) {
  //   data.map((item) => {
  //     if (item.day === "Monday") {
  //       monday.push = item.prediction;
  //     } else if (item.day === "Tuesday") {
  //     } else if (item.day === "Wednesday") {
  //     }
  //   });
  // }

  // return [monday, tuesday, wednesday, thursday, friday, saturday, sunday];
  return "sroter";
};
export default sorter;
